#!/usr/bin/perl

open (IN, "< lib/Text/EtText/EtText2HTML.pm")
	or die "cannot open lib/Text/EtText/EtText2HTML.pm\n";
while (<IN>) {
  /^\s*\$VERSION\s*=\s*\"(\S+)\"\;/ or next;
  print "$1\n";
}
close IN;
